{
  "name": "purescript-firebase4",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "eslint src/**/*.js",    
    "build": "purs compile bower_components/purescript-*/src/**/*.purs src/**/*.purs", 
    "postbuild": "pulp docs"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "eslint": "^4.5.0"
  }
}
